{{!-- trip details template --}}
{{!-- {{#unless isEditable}} --}}
  {{!-- <button {{action 'toggleEditable'}}>Edit</button> --}}
  {{!-- <h1>{{trip.location}}</h1>
  <h1>{{trip.title}}</h1> --}}
{{!-- {{else}}
  <h1>{{input value=trip.title}}{{/input}}</h1>
{{/unless}} --}}


{{#unless isEditable}}
  <button class='btn btn-primary'{{action 'toggleEditable'}}>Edit</button>
  <h1>{{trip.title}}</h1>
  <h4>Location:</h4>
  <h4>{{trip.location}}</h4>
  {{#if trip.arrival}}
  <h5></h5>
  <h5>{{trip.arrival}}</h5>
  {{/if}}


{{else}}
<p>Title</p>
<p>{{input value=trip.title placeholder='Title'}}</p>
<p>Location</p>
<p>{{input value=trip.location placeholder='Location'}}</p>
<p>Arrival</p>
{{jqui-datepicker value=myDate placeholder='Arrival' yearRange="2013:2015"}} {{myDate}}
<p>Departure</p>
{{jqui-datepicker value=myDate placeholder='Departure' yearRange="2013:2015"}} {{myDate}}
<button class='btn btn-warning' {{action 'updateTrip'}}>Save</button>
<button class='btn btn-default'{{action 'toggleEditable'}}>Cancel</button>
{{/unless}}
