{{!-- invite users template --}}

<div class='allusers'>
  {{#each model.users as |user|}}
    <div>
      <p>{{user.email}}</p>
      <button{{action 'addUser' user model.trip.id}} class='btn btn-primary invite-btn'>Invite</button>
    </div>
  {{/each}}
</div>
